// @flow
import colors from './colors';

const navBar = {
  navBarBackgroundColor: colors.primary500,
  statusBarTextColorScheme: 'light',
  navBarTextColor: 'white',
  navBarButtonColor: 'white',
};

export default navBar;
