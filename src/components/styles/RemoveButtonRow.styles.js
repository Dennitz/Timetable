// @flow
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  removeButtonSpacer: {
    width: 16,
  },
  rowContentContainer: {
    flexDirection: 'row',
    alignItems: 'center',
  },
});

export default styles;
